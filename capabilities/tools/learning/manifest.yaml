bundle_name: learning
version: '1'
tools:
  build_mdp:
    implementation_status: PLANNED
    inputs:
    - frozen task
    - RobotIR
    - approved observation/action/reward semantics
    outputs:
    - MDP definition
    supported_robot_families: &id001
    - tendon_driven_continuum
    fidelity: unimplemented; no numerical claims
    assumptions:
    - Requires Human-approved scientific semantics before implementation.
    applicable_when: Roadmap only; resolver returns IMPLEMENTATION_REQUIRED.
    limitations:
    - No callable and no fabricated results.
    failure_codes:
    - CAPABILITY_MISSING
    - PHYSICS_ASSUMPTION_REQUIRED
    cost:
      compute: unknown until implemented
    fields_consumed: []
    artifacts: []
    physics_contract_dependencies: []
    contract_status: PLANNED; consumed fields, artifact formats and physics dependencies
      require approval before promotion
    category: design_control_generation
    purpose: build mdp; unimplemented; no numerical claims
    version: round4_metadata_v1; implementation version remains source-bound
    states:
      implemented: false
      verified: NOT_IMPLEMENTED
      calibrated: false
    units:
      geometry: m
      time: s
      angle: rad
      force: N
      torque: N m
      dimensionless: counts/status
    coordinate_frame: world_base_x_forward_yz_cross_section for geometric data; not
      applicable to registry/artifact operations
    time_semantics: Static geometry has no trajectory time; run_task post-integration
      state differs from last dynamics force/length arrays; see observation sampling
      metadata
    dependencies:
    - Python
    input_example:
      status: NOT_IMPLEMENTED
      legal_executable_input: null
    evidence_refs: []
  train_policy:
    implementation_status: PLANNED
    inputs:
    - approved MDP
    - training settings
    outputs:
    - policy
    - training evidence
    supported_robot_families: *id001
    fidelity: unimplemented; no numerical claims
    assumptions:
    - Requires Human-approved scientific semantics before implementation.
    applicable_when: Roadmap only; resolver returns IMPLEMENTATION_REQUIRED.
    limitations:
    - No callable and no fabricated results.
    failure_codes:
    - CAPABILITY_MISSING
    - PHYSICS_ASSUMPTION_REQUIRED
    cost:
      compute: unknown until implemented
    fields_consumed: []
    artifacts: []
    physics_contract_dependencies: []
    contract_status: PLANNED; consumed fields, artifact formats and physics dependencies
      require approval before promotion
    category: design_control_generation
    purpose: train policy; unimplemented; no numerical claims
    version: round4_metadata_v1; implementation version remains source-bound
    states:
      implemented: false
      verified: NOT_IMPLEMENTED
      calibrated: false
    units:
      geometry: m
      time: s
      angle: rad
      force: N
      torque: N m
      dimensionless: counts/status
    coordinate_frame: world_base_x_forward_yz_cross_section for geometric data; not
      applicable to registry/artifact operations
    time_semantics: Static geometry has no trajectory time; run_task post-integration
      state differs from last dynamics force/length arrays; see observation sampling
      metadata
    dependencies:
    - Python
    input_example:
      status: NOT_IMPLEMENTED
      legal_executable_input: null
    evidence_refs: []
  evaluate_policy:
    implementation_status: PLANNED
    inputs:
    - policy
    - frozen benchmark
    outputs:
    - canonical evaluation evidence
    supported_robot_families: *id001
    fidelity: unimplemented; no numerical claims
    assumptions:
    - Requires Human-approved scientific semantics before implementation.
    applicable_when: Roadmap only; resolver returns IMPLEMENTATION_REQUIRED.
    limitations:
    - No callable and no fabricated results.
    failure_codes:
    - CAPABILITY_MISSING
    - PHYSICS_ASSUMPTION_REQUIRED
    cost:
      compute: unknown until implemented
    fields_consumed: []
    artifacts: []
    physics_contract_dependencies: []
    contract_status: PLANNED; consumed fields, artifact formats and physics dependencies
      require approval before promotion
    category: design_control_generation
    purpose: evaluate policy; unimplemented; no numerical claims
    version: round4_metadata_v1; implementation version remains source-bound
    states:
      implemented: false
      verified: NOT_IMPLEMENTED
      calibrated: false
    units:
      geometry: m
      time: s
      angle: rad
      force: N
      torque: N m
      dimensionless: counts/status
    coordinate_frame: world_base_x_forward_yz_cross_section for geometric data; not
      applicable to registry/artifact operations
    time_semantics: Static geometry has no trajectory time; run_task post-integration
      state differs from last dynamics force/length arrays; see observation sampling
      metadata
    dependencies:
    - Python
    input_example:
      status: NOT_IMPLEMENTED
      legal_executable_input: null
    evidence_refs: []

# 第四轮执行与验证安排

基线：`bf8d87e`，分支 `feat/round4-tools-visualization`。启动时只有用户提示词未跟踪；未发现适用 AGENTS.md。不重跑第三轮实验或历史全量回归。

阶段：①预算、授权与元数据；②统一离线回放和交互观察；③任务族与模型诊断；④一次宽区间配对实验及独立验证；⑤派生证据、结果报告和提交推送。

影响与必要验证：

| 变更 | 合并验证 |
|---|---|
| 扩展已有能力目录、范围授权 | 元数据入口存在性、非法参数及越权拒绝 |
| 新离线读取与动画 | 原始文件哈希不变、时间阶段、缺字段、失败轨迹、真实窗口交互及 GIF 解码 |
| 新任务族接口 | 种子分区、评价窗口、外力语义、三例正常结果 |
| 独立诊断版本 | 历史失败只读复查、一次代表输入、同输入小场景 |
| 新有限实验组织 | 配对条件一致、预留预算、恢复与封存校验 |

长度预算上限：24 个模型筛查，14 个长度、28 次 MuJoCo 尝试。粗阶段预留 20/20，细阶段预留 4/8（筛查/执行）；本轮拟用更少。验证预算上限：12 次 MuJoCo、24 次独立力学求解。调用前持久化预约；失败与中断消耗预算；不自动重试。账本独立保存于 `runs/round4/`，历史账本只读。只运行相关单元测试，失败最多两轮针对性修复验证；无全量回归计划。

观看、截图、视频和报告不启动数值后端。路径约束是应用检查，不是操作系统隔离。

完成记录：

- 首批7个离线测试：5通过，2受Windows临时目录ACL影响；调整为留存证据目录后，仅重跑这2项并合并3项受影响的历史元数据/授权检查，5通过。
- 增加明确中断恢复的扣账检查，1通过。补充缓存上下文和任务证据绑定后，运行2个新检查及1个直接受影响的任务评价检查，3通过。
- 实际窗口检查发现标题重叠，调整布局；同输入截图发现浮点时刻表示导致一帧错位，调整显示时钟，1项新时间对齐检查通过。最终14个不同测试通过，0次全量回归，单元测试后端调用0次。
- 一次开发验证：4次MuJoCo、5次求解/规划；三任务正常评价，独立同输入对照成功，历史代表输入返回有诊断的保护中止。最初MATLAB进程在沙箱中未启动成功，升级进程启动权限后成功；未发生重复求解。
- 正式长度一次粗阶段、一次细化：17个候选，4个几何拒绝，13次MATLAB规划、26次MuJoCo执行、0重试。停止条件按计划生效。
- 数值封存后仅添加缓存和任务来源保护、只读审计/打包入口及显示修正。旧数值和执行源码快照未改写；新任务来源检查离线重算三份已有评价，结果不变。
- 交付检查还修正了诊断版超时分支的单位：墙钟超时报告秒数，OutputFcn停止也标为资源保护；本轮未触发该分支，未为该报告字段更改追加后端调用。
- 原始证据审计一次PASS；随后针对新生成压缩包单独核对成员哈希，不重复数值审计。GIF只在派生图像改变后重新做必要解码/播放检查，始终0次后端调用。

可机读记录见 `docs/evidence/round4/test_record.json`、`budget.json`、`audit.json` 和 `package_check.json`。本轮未重置任何历史账本。

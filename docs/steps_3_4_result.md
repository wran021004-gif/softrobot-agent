# 步骤 3–4 开发结果（2026-09-17）

本轮完成已有串联双轴弯曲模型和两种控制模式的接口整理，没有加入新建模、优化或控制算法。

- 小修：完整模板先于离散校验选定，并支持模板完整离散；legacy 单段补齐真实设计/离散身份；公共设计编译器接收独立离散；平台过时错误码断言已修正。
- 公共实验：`experiment.json` 是任务、环境、安装、初态、外力和时间条件的唯一推荐来源；`execution.json`、`control.json` 分别选择模型/后端数值实现和控制。
- 步骤 3：新增 `ExperimentPolicy.dynamics_model`、注册模型定义、注册表模型—后端兼容关系和 1.1 后端专用数值契约。结果保存执行计划及模型、实现和数值身份。
- 步骤 4：控制被规范化为参考、算法、生命周期、采样时刻和执行器映射；确定性指令与末端反馈可只改公共配置切换。结果保存控制规范、区间开始观测、实际执行器命令和绳长目标。
- 兼容：旧 `family.parameters@1.0.0`、旧完整后端 Session 输入、`Segment.cells` 和旧单段入口继续支持，并明确标记兼容来源。
- 未实现：PCC/GVS、新模型转换器、规划器、轨迹优化、LQR、NMPC、移动平台、复杂接触和跨模型状态转换。

验证记录与双后端实际结果见 `runs/steps34_validation`。本轮完整动力学求解恰好 2 次：同一 structural 候选分别由 MATLAB 和 MuJoCo 完成 0.35 s、35 个控制/保存区间的积分；MATLAB 为 3651 个自适应数值步，MuJoCo 为 700 个固定步。两次评价都有效但都未达到 10 mm：误差分别为 0.02114479694 m 和 0.02115432108 m。后端末端终点差为 0.00009013470 m，轨迹末端 RMS 差为 0.00011098393 m。保存结果的比较、评价读取、控制/时序检查和 MuJoCo 回放材料检查均复用既有结果，额外求解为 0。

机器可读依据为 `candidate_checks.json`、`capability_catalog.json`、两个 structural 运行记录、`structural_comparison.json` 和 `saved_checks.json`。接口验收已完成；任务容差失败单独保留，不为此调参。

集中检查：候选/模型/控制与公共输入 13 项通过，平台核心 24 项通过，平台域与 legacy 5 项通过；Python 编译检查和 `git diff --check` 通过。另有 3 项真实 MATLAB 历史集成测试因未设置其专用环境变量而按原规则跳过；本轮要求的 MATLAB 实际积分已由上述 structural 运行完成。

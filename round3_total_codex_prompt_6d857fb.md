# ROUND 3 TOTAL CLOSEOUT：工具库与完整确定性改进实验

请在 `D:\softrobot-agent` 中执行本任务，基线提交为 `6d857fb`，原分支为 `feat/round3-1-debug-visualization`。直接审计、实现、验证、执行实验、封存、提交并推送功能分支，不要只交付计划。自行处理常规实现选择，不要每完成一步询问是否继续。

本文件是交给开发 Codex 的执行指令；当用户发送本文件要求执行时，下述新增授权在本轮生效。文件的生成本身不表示实验已启动、工具已实现或结果已验证。

## 1. 目标与完成语义

完成一次可审查的“设计搜索 → 仿真验证 → 控制对照 → 最多两轮有限改进 → 代表结果复现 → 证据封存”确定性 Harness 实验，同时补齐下一阶段受限 Engineer Agent 需要的核心工具。

必须分别报告：

- `toolchain_status`：核心工具是否实现并通过相应验证。
- `experiment_status`：预定实验、必要对照与证据验收是否完成。
- `canonical_task_status`：冻结任务评价器给出的机器人任务结果。
- `research_exit_decision`：下一阶段研究建议及证据，不自动等同于失败原因诊断。

机器人可以仍为 `TASK_FAILED`；这不要求重复运行。核心工具缺失、必要控制对照缺失、证据无法复核或后端未真实执行时，不得宣称整体完成。预算耗尽可以是合法停止原因，但不能把未完成的强制验收项标为通过。

本轮保持确定性 Python Harness，不接入 LLM、RL、MPC、自动 Skill 准入或真实设备控制。无需实现所有数学模型；交付下文规定的互补核心工具即可，不扩展为无限期模型库建设。

## 2. 开工审计与历史基线

首先检查当前分支、`git status`、适用的 `AGENTS.md`、本文件、README、现有日志、报告和真实入口。不要覆盖用户未提交改动。不要开工全测。

工作区干净且基线符合时建立 `feat/round3-total-closeout`；分支已存在则检查并继续，不能重置已有工作。HEAD 已向前推进时核对差异并复用已完成工作，禁止为了匹配基线回退代码。

优先读取：

- `docs/round3_final_result.md`、`docs/round3_experiments.md`、`docs/task_contract.md`、`docs/debug_visualization.md`。
- `tasks/reach_free/contract.yaml` 及其引用源。
- `capabilities/robot_families/tendon_driven_continuum/envelope.yaml`、对应 grammar、`configs/experiments/round3_final_human_authorization.md`。
- `tools/experiment_tools.py`、`tools/experiment_policy_tools.py`、`tools/round3_studies.py`、`tools/harness.py`。
- `tools/pcc_math.py`、`tools/actuation_tools.py`、`tools/shape_tools.py`、MATLAB 实现。
- `controllers/pcc_tip_feedback.py`、`schemas/experiment_policy.py`、`tools/mujoco_tools.py`、`tools/debug_tools.py`、证据与 trace 实现。

历史报告记录：

- 长期长度包络 0.05–0.80 m。
- 最佳已观测设计：长度 0.29 m、腱布置半径 0.018 m、8 根腱；身体半径 0.020 m、离散段数 8、连续体段数 1。
- MATLAB 预测误差约 0.0152216443 m；MuJoCo 实际误差 0.1089206160124638 m，`TASK_FAILED`。
- 原基线长度 0.40 m、腱半径 0.015 m、4 根腱，实际误差约 0.1711616689 m。
- 五组研究新增 63 次候选评估，其中 26 次 MuJoCo；历史全量回归 133 项通过；研究出口为 `PHYSICS_MODEL_REFINEMENT_REQUIRED`。

以上是报告基线，不等于本轮已经独立核验。检查本地原始运行目录、manifest、配置、轨迹和哈希，将历史证据标为 `VERIFIED_LOCAL`、`REPORT_ONLY` 或 `MISSING/INVALID`，不得由报告数值补造原始数据。`runs/` 被忽略并不表示本地一定缺失。

不重跑旧的长度、半径、腱数、联合搜索或离散化全套研究。缺少可复用证据时仅在本轮参考预算内补跑必要参考点。

先写能力矩阵和执行计划，再开发。矩阵列出工具入口、输入输出、单位坐标系、参数权威源、依赖、假设、限制、实际实现状态、验证证据、标定状态、后端调用与副作用。区分 IMPLEMENTED、VERIFIED、CALIBRATED；只有 manifest 或 stub 不算实现。

## 3. 本轮授权、设计范围与冻结边界

### 3.1 主实验的固定真值

继续使用 `reach_free_v1`，目标与容差从权威任务文件读取。当前应为目标 `[0.25, 0, 0.15] m`、末端最终误差不超过 0.01 m；时间步长 0.002 s、1000 步、2 s 末态评价。代码不得另写一份任务真值。

保留原任务、环境、初态实现、评价器、Gate 语义、`legacy_v1_surrogate`、执行器增益与受力限幅、数值设置和基线 DesignSpec。不得通过改容差、延长仿真、改变重力、初态、物理系数或评价时刻取得成功。

TaskContract 继续引用真值，不保存候选设计、优化结果或重复目标值。DesignSpec 描述候选，RobotIR 展开共同几何与显式物理字段。新增分析模型拥有独立参数版本，不得把分析模型参数偷偷写回主实验 RobotIR 或 MuJoCo。

### 3.2 可搜索设计空间

| 参数 | 主实验权限 |
|---|---|
| total_length_m | 0.05–0.80 m |
| tendon_routing_radius_m | 0.002–0.018 m，且严格小于身体半径 |
| tendon_count | 整数 3–8 |
| body_radius_m | 固定 0.020 m |
| segments | 固定 8 |
| sections | 固定 1 |

这是 surrogate 设计探索授权，不是制造或真实材料有效性证明。不要采用前面旧草案的 0.10–1.00 m 等范围覆盖当前包络。离散段数不参与性能排名或设计优化：现有每关节参数和 capsule 质量随分段变化会改变等效力学。

### 3.3 本轮新增的 C2 与有限改进授权

本执行指令明确授权：在上述冻结主实验中使用现有 C1 和固定参数 C2，对照原基线与阶段性最好设计，并执行最多两轮基于保存证据的确定性改进。禁止控制器调参搜索。

在 `configs/experiments/` 新增本轮授权记录和机器可读策略，准确引用本指令、参数、预算和范围。现有 `ENVELOPE_SUBSET` 路径会拒绝 C2、feedback_parameters 和 repair_actions，必须实现有范围约束的新授权路径；不能删除校验、把所有策略默认视为批准、将 TEST_ONLY 改标签后用于正式任务，或追溯改写旧授权。

可增加必要 schema、策略校验与 grammar 的本轮授权引用；保留旧包络、旧策略及默认拒绝行为，不扩大所有实验的 C2 权限。完成授权实现后，将正式实验实际使用的源与哈希冻结。

C2 使用如下唯一固定配置，整个主实验不调整：

```yaml
feedback_parameters:
  gain_rad2_per_m2: 0.3
  update_every_steps: 20
  max_bend_update_rad: 0.01
  max_command_update_m: 0.0001
  min_tendon_length_m: 0.001
  max_tendon_length_m: 0.900
controller_parameter_bounds: []
```

前四项沿用已有开发夹具的设置；后两项是本轮显式给定的命令边界，用于覆盖当前设计包络，不代表实际硬件行程。它们不能替换冻结的 MuJoCo 执行器受力限制。正式对照前校验初始 PCC 命令、腱顺序、8 腱支持及编译出的实际控制限制。

记录反馈来源、20 步对应的 0.04 s 更新周期、误差、内部弯曲参考、命令、每次截断以及执行器受力。C1 是保持 MATLAB 腱长命令，内部长度伺服仍工作；C2 是额外的末端反馈，不是动力学逆解。参数来源属于实验设置，不声称经过稳定性证明或调优。

本轮另授权实现和验证独立的含受力模型、张力执行和扰动工具；全部标为分析/工具验证，不更换正式排名的物理对象。真实材料与硬件参数标定仍需真实数据和另行研究。

## 4. 核心数学工具：真实实现与明确假设

优先复用已有实现。允许采用 Python 科学计算实现新模型，不强制所有模型都放入 MATLAB；保留并真实验证现有 MATLAB M0/M1 路线。新依赖必须必要且记录版本。

### 4.1 PCC 几何

交付正运动学、末端位姿、中心线采样、有界逆解或目标拟合、任意已支持腱数的腱长映射、末端与腱长 Jacobian。核验直线极限、小弯曲、边界与不可达目标、坐标系和有限差分一致性。

现有 `pcc_math.py`、MATLAB、actuation 与 shape 工具已有能力直接复用，避免两个互相漂移的几何真值。相同长度不同半径的 MATLAB 末端误差相同不能成为丢弃半径对照的理由。几何可达不代表受力后可达。

### 4.2 含受力准静态模型

至少实现一种低阶离散杆或弹性梁模型，可使用与 RobotIR 相容的双轴弯曲离散坐标，无需完整 Cosserat/FEM。

必须真实包含弯曲刚度、重力、末端外力、腱张力及其广义力/力矩，输出平衡形状、末端位置、平衡残差、迭代次数、收敛状态与失败类别。

写明自由度、夹持边界、势能或平衡方程、力与力矩的符号、作用点与坐标系；张力可经腱长对广义坐标的导数产生广义力，必须验证符号及力臂。不能只把重力和负载放进元数据。

明确是否忽略伸长、剪切、扭转、摩擦、松弛和接触。残差不收敛不得返回成功。参数来源缺失时使用命名的 `SYNTHETIC_UNCALIBRATED` 示例配置，写明单位与来源，不把示例当材料标定。

### 4.3 低阶动态模型

复用准静态模型的坐标与力学定义，加入质量/惯性、阻尼、初态和时变输入，真实积分输出状态和末端轨迹。记录积分方法、步长/容差、求解状态和能量适用性。

至少执行三个小例子：有阻尼的自由衰减或静置、时变腱张力响应、外力扰动响应。无外部能量输入的阻尼系统检验合理衰减；重力势能计入能量判断，不能要求所有位置坐标单调下降。验证预算和数值容差在运行前声明，不通过修改验收阈值掩盖失败。

### 4.4 模型比较与有界辨识

交付同一设计下 PCC、准静态、低阶动态和 MuJoCo 的可执行比较入口，输出末端偏差、按一致弧长/坐标定义的中心线差异和适用的动态指标。

强制标记输入语义：腱长位置命令与张力输入不是等价输入。PCC 不求受力平衡，不能伪造 PCC 的张力响应。无法等价比较的组合返回明确的不可比较原因；仍可做已说明条件差异的描述性对照。

至少完成一条受力模型与独立 MuJoCo 张力测试模型在相同张力时序、重力、外力及可说明参数映射下的真实对照。该测试模型使用独立版本，不替换正式主实验 XML。

提供有界刚度或阻尼参数拟合入口，目标函数、边界、数据来源、训练/验证划分、停止规则和求解预算入证据。用独立激励或独立初态的验证轨迹，不能随机拆同一时间序列后声称独立验证。至少完成合成数据的参数恢复与留出验证，报告可辨识性限制和是否碰边界。

仅合成或 MuJoCo 数据时称“工具验证/仿真间拟合”；PCC/动力学工具得分和拟合结果不能改变 canonical gate 或自动触发物理参数更新。

## 5. 物理映射与 MuJoCo 工具链

审计总质量与分段质量、惯性、刚度和阻尼与段长的关系、腱布线与力臂、长度与张力、限幅、初态、坐标系和符号。现有同名参数不能自动视为跨后端等价。

如独立分析模型使用 `EI / 段长` 等映射，给出其离散能量推导和适用条件；未知映射显式标记。若建立改进 MuJoCo 映射，新增版本并保留旧版本可执行，独立实验不得进入旧物理排名。

确认或补齐以下实际入口：

1. DesignSpec → RobotIR → XML，结构、腱顺序、参数范围和真实模型加载验证。
2. 初态建立、headless 确定性执行、seed、运行版本、完整数值设置记录。
3. 腱长控制，以及独立版本中可执行的张力控制；说明 actuator 语义和 pull-only 约束，不把位置伺服的 ctrl 数值直接当牛顿。
4. 至少一种世界坐标系外力扰动注入：对象、作用点、起止时间、幅值、单位与清零行为有记录。平台运动若不支持就明确列出，不为凑功能添加无关模型。
5. 记录末端轨迹、qpos/qvel、腱长、执行器力/可获取的腱力、命令、限幅、接触和警告。不能混淆 actuator_force 与全部腱内力。
6. NaN、发散、后端异常、模型/求解失败、执行器饱和与预算耗尽的结构化结果。饱和属于观测，不能自动诊断唯一失败原因。
7. 轨迹图、导出、viewer，以及基于保存 XML 和状态序列的可视化回放。回放不再次执行正式 `mj_step`，必要的状态前向计算只用于可视化；支持的状态字段与限制要写清。
8. 有预算的批量执行、候选边界恢复和部分结果保存。渲染/绘图故障不得丢失或改写已完成的物理结果。

能量诊断只有在其物理定义和 API 取值可靠时启用；不支持则返回原因。图像是辅助证据，冻结评价器依赖正式数值产物。

## 6. 跨层接口和 Windows 脚本

复用现有 ToolResult、RunArtifacts、TraceWriter、resolver 和 failure taxonomy，不再建一套平行框架。补齐真实可调用工具登记、输入输出 schema、错误类别、耗时、证据引用、能力限制和预算成本。

resolver 不得静默从受力模型降级为 PCC，也不得把未实现工具标为可用。为下一阶段 Agent 提供可查询的工具列表和合法输入示例；权限元数据不是 OS 安全隔离，不能宣称已具备安全自主写代码能力。

新增真实后端调用、静态/动态求解和拟合内层调用均应可统计；高频内部细节可写独立 ledger，由高层 trace 引用。模型、控制器和参数版本进入运行证据。

提供 PowerShell 可用的环境检查、数学例子、工具查询、主实验启动/恢复、结果导出、离线审计、绘图和回放命令。检查现有 Python/Conda、MATLAB Engine、MuJoCo、版本与输出目录；不自动重装整个环境。后台辅助进程使用隐藏窗口，需要用户观看的 viewer 才可显示。

硬件未知时提供真实日志回放适配器及 mock 验证，含观测/动作、时间戳、单位、连接状态、停止接口和错误路径，明确这不是设备驱动。不得自动连接真实电机或机械臂，不添加无设备需求的 ROS 驱动。

## 7. 实验预算、计费与恢复

### 7.1 正式实验硬上限

| 阶段 | 新增 M0/M1 筛查评估上限 | MUJOCO 路线尝试上限 |
|---|---:|---:|
| A 参考设计 | 4 | 4 |
| B 有限搜索 | 16 | 4 |
| C 同设计 C1/C2 对照 | 0 | 4 |
| D 最多两轮有限改进 | 8 | 4 |
| E 代表复现及故障恢复 | 0 | 4 |
| 合计 | 28 | 20 |

最多 48 次新增路线评估；MUJOCO 路线内部包含 MATLAB 分析，不重复算成独立候选评估，但底层 MATLAB 实际调用另计。不同控制器、重试和代表复现都算新尝试。拒绝、失败和中断也消耗该路线的预留尝试，不能只统计成功次数。

已完整核验的缓存复用不消耗新数值调用，但必须记录 cache hit、原 run、配置与哈希。不得将重复启动伪装为复用。不要求跑满预算。

各阶段额度预先分开；搜索不得借用 C/E 的保留额度。E 的 4 次中至少留 1 次给代表复现，另外最多 3 次用于有明确可恢复原因的重试。不得自动扩容或把正式候选伪装成开发测试。

### 7.2 开发、拟合和集成验证单独记账

先制定 `validation_budget`，列出聚焦测试、数学例子和集成验证的有限上限。新增开发/例子的真实后端预算上限为 MATLAB 工具调用 40 次、MuJoCo 执行尝试 40 次；拟合最多 2 次有界拟合任务，每次至多 100 次目标函数评估，含有限差分所需调用；合成数据生成与留出验证另预留最多 40 次模型求解。

全量回归仅一次，作为独立验证账目：运行前依据测试代码/收集结果列明会启动的后端，运行时统计实际调用；不能用全测掩盖额外科研搜索。其他小型数学测试也记录运行命令和执行量。预算不足时保留产物并如实说明，不能无限重试。

### 7.3 候选边界恢复

正式启动前保存 experiment_id、冻结策略、候选生成规则、seed=17、阶段预算、确定性决策规则和源代码指纹。run ID/时间戳可以变化，决策顺序不能依赖墙钟时间或无序集合。

持久化 attempt 的 RESERVED/STARTED/COMPLETED/FAILED/INTERRUPTED 状态。先原子预扣预算并落盘，再启动真实调用；并发启动要有实验锁。中断后保守计费，已封存子 run 不重写，未完成尝试保留，重试用新 attempt 并链接旧记录。

恢复先核对策略、冻结源、版本、候选与预算；变化不兼容时停止并说明。缓存键包含设计、IR、任务/环境、初态、物理/数值设置、控制器及参数、后端与执行代码版本；只有数值相同或路径相同不够。历史跨提交复用需说明影响执行的源是否等价，否则按历史参考处理并在预算内重建。

支持只读 plan/dry-run，显示剩余阶段及预算，不启动 MATLAB/MuJoCo。正式完整实验只能启动一个逻辑 experiment；中断后用 resume，不能重开一轮掩盖失败。

## 8. 正式实验步骤与确定性决策

### A. 参考设计

核验并优先复用原基线、历史最好设计；需要时加入上一轮长度 0.35 m 参考和解析几何匹配长度约 0.3062377168 m 的参考。解析值从现有工具和任务真值计算，不作为手写的新任务数值。

两项强制参考是原基线和历史最好设计。相同配置的有效证据可复用；缺失则在 A 的预算内生成。分别保存模型预测、实际评价、诊断与证据状态。

### B. 少量有依据的宽包络筛查与 C1 验证

不重复旧研究网格。启动前固化最多 16 个新筛查候选的规则，覆盖长度与半径变化、合法边界和若干腱数，重点利用已有证据选择未评估邻域。保留宽包络定义，不要求对明显不可达点反复运行后端。

候选去重、排序、seed 与冲突处理固定。现有 HARD 几何门可拒绝；仅 MODEL/SCREENING 不达标不能当成新的 HARD 门。

最多 4 个 C1 MuJoCo 候选必须同时兼顾：模型预测较好、相同长度与腱数下不同半径的实际对照、结构覆盖。选取规则预先写明；不得仅按 MATLAB 误差排名而忽略半径/腱数效应。复用的有效对照可满足覆盖要求，未覆盖要如实标记。

### C. 原基线和阶段性最好设计的 C1/C2 配对

固定两个设计：原基线，以及 A/B 结束时 canonical C1 实际表现最好的设计。先冻结它们的 ID，再进行反馈对照，避免事后挑选。

每个设计都必须有同条件 C1 和 C2：任务、环境、设计、编译物理、初态、仿真时间、步长、seed 和评价器相同，仅外层控制方式不同。C1 可在严格证据条件满足时复用，否则在 C 的 4 次额度内补跑。

比较实际末态误差、误差时间曲线、命令/跟踪、限幅、受力和数值警告。C2 改善只能证明该固定控制器在该次条件下表现较好；不得断言原失败全部来自控制器。变化命令轨迹不能与初始 PCC 形状当作全程相同命令对照。

### D. 最多两轮有限改进

开发结束、正式启动前，把以下原则细化为机器可执行的规则表，写明步长、候选顺序、去重和 tie-break；运行中不得人工改规则追分：

1. 每轮读取已封存的实际评价和诊断，保存决策时可见的证据集合。
2. 对数值/工具异常先标识结果不可用于性能排序；仅对明确可恢复的执行错误使用 E 恢复额度。TASK_FAILED 不是重试理由。
3. 基于当前有效最好设计生成至多 4 个新 M1 候选、评估至多 2 个 MuJoCo 路线组合。控制器只能从已授权固定 C1/C2 中选择，不调参数。
4. 局部扰动的方向和步长、半径效应的比较要求、腱数选择及模型筛查后的覆盖选择，都从预先规则和已保存观测决定。未知因果仍写 UNKNOWN。
5. 新模型诊断至少真实接入一次：Harness 对选定设计调用新增准静态/动态或模型比较工具，保存 ToolResult、trace、参数与成本，再由规则读取它作为解释或有明确边界的候选选择依据。不得用新模型未标定阈值硬拒绝 canonical 候选，也不能仅以外部脚本演示声称已跨层接通。
6. 始终保留 incumbent；canonical PASS 优先于 FAIL，其次用冻结实际误差排序，完全同分按稳定候选 ID 和控制器 ID 决胜。缺失、NaN、运行失败不能作为更优结果。相同设计不同控制器作为不同 evaluated pair 记录，同时单列 C1 设计排名。

每个 decision 记录规则 ID、输入证据、候选、选/未选理由、预算前后值与结果。最多两轮；无新候选、动作耗尽或预算不足即可停止。无需强求数值改善，也不能把按预定规则尝试描述为已确定失败机制。

### E. 代表复现与封存

选择最终有效最好设计/控制组合，从已保存配置真实复现一次，计入 E。复现前固定同机同版本数值一致性容差，区分文件字节哈希验证与浮点轨迹一致性。数值容差只用于复现验收，不能改任务的 1 cm 标准。

报告末态误差差值、关键轨迹最大差值、任务状态与数值状态一致性。不得只比较两个汇总 JSON 就称为真实复现。一次不一致保留证据并诊断，不以反复运行挑一个吻合结果。

出现 canonical PASS 可以提前终止非必要搜索，但不能跳过强制的双设计控制配对、代表复现和证据封存。

## 9. 可信且可远程审阅的证据

正常运行产物仍放 `runs/<id>/`，保留不可改写的子 run、父研究关系和封存哈希。另生成可随 Git 提交的 `artifacts/round3_total_closeout/<experiment_id>/` 精简证据包，不把整个 runs、MATLAB 缓存或环境二进制加入版本库。

证据包至少包含：

- manifest、实验协议与授权、冻结源/配置哈希索引、实际执行源码快照或可核验的内容指纹、运行版本、种子。
- 全部候选表、DesignSpec/IR 引用、输入语义、来源阶段、模型预测、canonical 结果、失败类别、复用关系与选择依据。
- 阶段决策、预算 ledger、底层调用统计、停止原因、恢复关系、全部子 run 的 manifest 与关键数值摘要。
- 原基线、阶段性最好设计的 C1/C2、最终最好设计以及复现 run 的任务/环境/设计/IR/控制配置、XML、最终状态、评价与诊断原始数值、可重算的末端轨迹与关键命令/力记录。
- 数学验证、受力模型比较、拟合训练/留出数据与结果、集成验证、一次全量回归日志和任何失败项重测记录。
- 关键曲线、生成脚本和证据索引。正式指标来自全精度数据；绘图可降采样并记录方法，不以图片替代数值。

目标总大小不超过 20 MiB。优先消除重复源快照、按内容哈希共享文件、压缩数值文本；不得为满足大小静默丢弃必需审计数据。若确需超出，先说明组成和保留必要性，勿混入无关大型产物。

每个被报告为“已核验”的关键数值必须能追到包内真实文件。摘要哈希不等于具备原始证据：未打包的原始文件明确标记 external/local-only，不让离线审计误报为完整核验。

提供无需 MATLAB/MuJoCo 的离线审计命令，允许依赖项目普通 Python 数据处理依赖：

1. 检查包内文件哈希、引用完整性和封存关系，拒绝缺文件与路径越界。
2. 从保存任务真值和实际末端位置重算误差及 canonical 判定；覆盖候选排名与代表控制对照。
3. 从完整尝试 ledger 重算阶段/总预算，核对失败、中断、重试、复用和实际底层调用；检查阶段额度不被侵占。
4. 检查对照的冻结字段一致性、候选范围、C2 参数固定、设计/控制排名与停止规则。
5. 核对代表复现数值一致性与源码/版本关系；不能重执行后端。

报告必须区分“完整轨迹审计”“关键数值审计”“仅外部文件哈希声明”。哈希提供内容一致性，不是独立实验见证或防止整个证据包被替换的签名。

## 10. 测试纪律与执行顺序

严格执行：

- 开工不全测；开发仅运行直接相关测试和小例子，不为低影响文档编辑写镜像测试。
- 不把历史 133 项通过当作新代码通过证据。
- 优先覆盖真实风险：PCC 极限/Jacobian，静态残差与受力方向，阻尼响应，独立拟合验证，XML 真实加载与张力/位置语义，C2 8 腱，授权拒绝，预算预留，候选边界恢复，证据缺失/篡改检测，跨层成功执行与结构化失败。
- 集中安排新增真实 MATLAB/MuJoCo 集成验证，避免每阶段反复启动。能复用 MATLAB 会话则复用，保留实际调用次数。
- 实现收敛后，全量回归最多一次，启用真实 MATLAB/MuJoCo 的现有开关，例如 `$env:SOFTROBOT_TEST_MATLAB = "1"` 后执行项目的完整 unittest 命令。记录精确命令、版本、测试数、跳过项、退出码和日志。
- 全测失败只修复并重跑失败项及直接依赖，不运行第二次全量回归。后续新改动按相关测试验证，并如实说明全测对应的代码状态。
- 一次全测与必要集成验证通过后冻结正式执行代码/策略，再启动一个完整主实验。若后端不可用，记录阻塞，禁止以 mock 冒充真实验收。
- 正式中断从候选边界恢复。执行代码必须修复时保留旧快照、明确新版本影响及哪些已完成结果仍可复用，不能静默把两套物理或控制实现合并排名。
- TASK_FAILED、报告/文档修改、绘图、导出和离线审计失败都不得触发整轮重跑；从已有数值产物修复后处理即可。

本轮要求的是最终可信交付，不是重复运行次数。必要测试失败或外部不可恢复问题须明确说明剩余缺口；不得宣称完整成功后把核心验证留作下一步。

## 11. 最终文件、提交与汇报

更新 README 和现有项目日志，修正“优化/C2 未实现”等过时描述并标明历史阶段；不改写历史数值或原结论。文件路径可按现有结构合理复用，但提供清晰入口：

- 工具能力矩阵。
- 模型方程、假设、物理参数来源及跨后端映射说明。
- Windows 环境检查、工具/例子、主实验启动与恢复、离线审计和回放指南。
- 本轮授权、策略、阶段预算、确定性规则。
- `docs/round3_total_closeout_result.md`。
- 可提交的精简证据包及证据索引。

提交前检查改动清单、diff、暂存区、凭据与产物大小。仅提交本轮成果，禁止 force push，不合并 main。本指令授权完成后 commit 并 push 当前功能分支；不重新询问已经授权的普通提交推送动作。保留用户不相关未提交改动。

报告实际 commit hash、分支与 push 结果；push 失败保留本地提交，给出具体手动恢复命令，不能声称远程已更新。运行时源码可能尚未提交，使用执行快照/内容指纹准确关联最终提交，禁止伪造运行时 commit 为事后提交。

最终用中文，从项目负责人/研究者视角说明：

1. 哪些工具新增了实际能力，它们各回答什么研究问题，哪些已实现/已验证/未标定。
2. 本轮实验完整程度、设计与控制分别是否改善、最终实际误差和 TASK 状态；原始失败原因仍未知时明确 UNKNOWN。
3. MATLAB、独立数学模型与 MuJoCo 分别真实模拟什么，哪些参数是 surrogate 或仍无映射。
4. 实际使用的预算、测试结果、正式 experiment ID、停止与恢复原因、代表复现和离线审计结果。
5. 关键文件与一组可以复制执行的查看、审计、回放命令。
6. 下一步接受限 Agent 还有哪些具体阻塞，哪些属于之后的真实物理标定研究。
7. 未完成项、git status、commit 与 push 结果。

不要将“工具库已可调用”写成“模型已被真实机器人验证”，不要将“实验执行完成”写成“任务达成”，也不要因机器人仍然 TASK_FAILED 而无限追加实验。

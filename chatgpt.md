不是站在 Codex 作者的角度解释代码，而是站在项目负责人/研究者的角度，帮助你掌握这个系统。

以后你让我“查看一下现在进展”“解释这个提交”“看看 Codex 做了什么”时，我会优先从远程仓库实际代码和运行报告出发，重点给你讲清楚这些问题：

当前任务到底是什么，哪些内容已经冻结，哪些只是开发测试；
TaskContract（任务契约）到底引用了哪些真值；
当前软体机器人允许设计到什么范围，哪些参数可变、哪些固定；
DesignSpec（设计规格）和 RobotIR（机器人中间表示）各自负责什么；
MATLAB 当前实际用了什么数学模型，读了哪些机器人和环境参数；
MuJoCo 当前真实模拟了什么物理，又有哪些只是近似参数；
MATLAB 与 MuJoCo 的结果为什么可能不一致；
Controller（控制器）到底执行了什么，而不是只看名字叫 C1/C2；
哪个参数从哪里来的，是任务参数、机器人参数、物理参数、控制参数还是数值仿真参数；
当前一次失败是“机器人设计不行”“数学模型预测不好”“控制器不行”“执行器受限”，还是目前只能写 UNKNOWN（未知）；
runs/<id>/ 里面哪些文件最值得你调试时先看；
当前 Round 做完以后，离论文中的完整 workflow 还差什么；
哪些内容已经可以交给 Agent，哪些仍必须由你作为 Human 决定。

同时我会尽量把代码里的英文名称翻译成人容易理解的话。例如以后我不会只说：

ExperimentPolicy 没有授权。

我会说：

ExperimentPolicy（实验策略文件）负责规定“允许优化哪些机器人参数、这些参数能改多大范围、允许跑多少次 MATLAB/MuJoCo、允许使用哪种控制器”。现在代码已经会执行优化，但正式实验的这些范围还没有由你批准，所以只能在测试环境里证明“程序能优化”，不能把测试结果当科研结果。

类似地：

TaskContract → 任务契约/任务真值索引
DesignSpec → LLM 或人工提出的机器人设计方案
RobotIR → MATLAB 和 MuJoCo 共用的机器人结构描述
EnvironmentSpec → 环境唯一真值
ExperimentPolicy → 某一轮实验允许怎么搜索和试验的规则
ToolResult → 某个工具执行后留下的结构化结果
Trace → 这一轮到底按什么顺序做了什么
Artifact → 真正产生的文件和数值证据
Gate → 不能由 LLM 覆盖的程序化裁判
CandidateFinding → 从一次运行里提取出的观察事实，还不是经验规律
Skill → 经过多次验证并人工批准后才能复用的工程经验


给 Codex 的 Prompt 应该明确禁止“反复全测”
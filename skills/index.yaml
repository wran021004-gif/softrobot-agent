schema_version: 1
registry: skills.registry.SkillRegistry
directories:
  candidates: candidates
  approved: approved
  deprecated: deprecated
default_status: approved
storage: immutable_versioned_revisions
